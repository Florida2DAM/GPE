/**
 * @format
 */

import {AppRegistry} from 'react-native';
import OrderConfirmsScreen from './screens/VisitScreen';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => OrderConfirmsScreen);
